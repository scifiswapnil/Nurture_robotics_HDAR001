diff_swerve_controller:
  ros__parameters:
    # Kinematics
    wheel_base: 0.5
    track_width: 0.5
    
    # Check limits
    max_speed: 2
    
    # PID for Steering
    steer_kp: 0.005
    steer_ki: 0.0005
    steer_kd: 0.0
    
    # Topic names for motors (assuming typical Gazebo format or generic)
    # Output topics will be constructed like: /commands/{joint_name}
    # where joint names are derived or configured.
    # Actually, simpler to just hardcode standard names for now.
